var listaDeCarros = [ "Fox", "Uno", "Gol", "Astra", "Fiesta"]

var frase = "Nome do Carro: "

var numeroDePosicoes = listaDeCarros.length

for (let i = 0; i < listaDeCarros.length; i++ ){

   console.log(frase + listaDeCarros[i])

}  